// Ztorrent v0.2 runtime configuration.
// Accelerator backend is REQUIRED. Browser-only fallback is intentionally disabled.
window.ZTORRENT_CONFIG = {
  API_BASE_URL: "",
  REQUIRE_ACCELERATOR: true,
  POLL_INTERVAL_MS: 750
};
